<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Points Android at the asset statements above. Without this the
             system has nothing to verify and refuses the relying-party id. -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Hunt is a GPS game, and in a WebView that is not a page-level concern.
         Capacitor's BridgeWebChromeClient.onGeolocationPermissionsShowPrompt
         answers navigator.geolocation by launching a RUNTIME request for
         exactly these two permissions. Android denies a runtime request for a
         permission the manifest never declared — immediately, with no dialog —
         so without these lines the callback is invoked with `false` and every
         fix fails as PERMISSION_DENIED. The page cannot tell that apart from a
         player refusing, so it is silent as well as fatal.

         Coarse is listed alongside fine because that handler accepts coarse
         alone on Android 12+ (a player may grant "approximate"), and because a
         fine-only declaration makes the approximate/precise toggle disappear. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
