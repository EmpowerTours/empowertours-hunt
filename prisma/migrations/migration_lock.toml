# Generated by Prisma and committed with the migrations it locks.
#
# Missing until now: the init migration was hand-authored, and without this file
# `prisma migrate diff --from-migrations` cannot tell which connector the
# directory targets. `migrate deploy` happens to work regardless, so the gap was
# invisible from the deploy path alone.
provider = "postgresql"
